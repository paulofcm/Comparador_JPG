from tkinter import *

menu_inicial = Tk()
menu_inicial.title("Comparador de JPG")

menu_inicial.geometry("500x250+400+200")
menu_inicial.resizable(True, True)

menu_inicial.minsize(width=500, height=250)
menu_inicial.maxsize(700, 400)

menu_inicial.mainloop()
